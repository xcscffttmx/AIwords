# Caddy 配置：唯一对外入口，自动为 {$DOMAIN} 申请并续期 Let's Encrypt 证书
# DOMAIN / ACME_EMAIL 从 docker-compose 的环境变量注入（见根目录 .env）
# 本地或未配置域名时 DOMAIN 默认为 localhost，Caddy 会使用内部自签证书
{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode gzip
	# 反向代理到内部的前端 Nginx（Nginx 再把 /api、/health 转发给后端）
	reverse_proxy frontend:80
}
